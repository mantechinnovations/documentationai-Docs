---
title: 'Authentication '
description: At this point there is no authentication setup to the Twidget APIs
---
## Base Address of End Point

<Callout kind="info" collapsed="false">
  <a href="https://d300b.twidget.io/" target="_blank" rel="noopener noreferrer">https\://d300b.twidget.io/</a>
</Callout>



Call the APIs documented in the "Endpoints" section below. Expand the node "Twidget Endpoints". You will see all required Endpoints for the provider app.&#x20;

<Callout kind="info" collapsed="false">
  Note that the twidget endpoints acts a proxy to the original "http" endpoints
</Callout>

The API are connect to the SQL Server backend. The Twidget endpoints are a subset of the many apis available across web, mobile and chatbot.

##

<Expandable title="Why am I getting a 401?" default-open="false">
  Verify that:

  - The token is valid and not expired.

  - The token is sent in the `Authorization` header.

  - The header value includes the `Bearer` prefix and a space before the token.
</Expandable>

